# MCP Server — Railway 배포용 의존성
# Python 3.12 이상 필요

# MCP framework
mcp>=1.27.1

# Supabase client
supabase>=2.30.0

# Environment variable loader
python-dotenv>=1.2.0

# HTTP client (supabase 의존성, 명시적 고정)
httpx>=0.27.0
